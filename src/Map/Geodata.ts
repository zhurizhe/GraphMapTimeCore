export class Interaction {
    public static zoomMap(scale: number): void {
        // Implementation of map zoom
    }

    public static dragMap(position: { x: number, y: number }): void {
        // Implementation of map dragging
    }

    public static addMarker(marker: any): void {
        // Add a marker to the map
    }
}
